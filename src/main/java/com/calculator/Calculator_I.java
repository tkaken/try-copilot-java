package com.calculator;

public interface Calculator_I {
    int add(int firstNumber, int secondNumber);
    int subtract(int firstNumber, int secondNumber);
    double divide(int dividend, int divisor);
}
